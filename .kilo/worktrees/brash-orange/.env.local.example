OPENAI_API_KEY=
# Optional: point at a compatible gateway/router instead of api.openai.com
OPENAI_BASE_URL=
# Optional: model name/alias to use (falls back to OPENAI_MODEL, then gpt-4o-mini)
OPENAI_MODELS=
